# Archived results

The outputs of the run behind the manuscript, so a reader can check any reported value
without re-running the pipeline.

| Path | Contents |
|---|---|
| `canonical_results.csv` | one row per (`run_id`, metric): headline run, rolling origins, contamination sweep, split comparison. 7,372 rows, 532 `run_id`s |
| `manifest.json` | library versions, seed, grids and caps of the run |
| `table_*.csv` | the analyses written by their own cells: aggregation schemes, PaySim ablation, label efficiency, bootstrap intervals, operational measurements, hyperparameter and subsampling sensitivity |
| `manuscript_tables/` | the same tables in the layout used in the paper, rebuilt by `../scripts/make_manuscript_tables.py` |
| `figures/` | the figures as published |

Not included: `test_scores.npz` (83 MB of raw score vectors), regenerated by cell 13 of the
notebook. The comparison table against prior work is a literature table, not an output of
this pipeline, and is kept in the manuscript only.

To regenerate everything, set the two dataset paths and run the notebook top to bottom;
outputs are written to `revision_v2/` and can be diffed against this directory.
